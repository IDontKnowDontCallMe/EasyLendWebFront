import React from 'react';
import { Form, Input, Button } from 'antd';

class AuthCompletedMention extends React.Component{

  render(){

    return (
      <div>身份验证已完成!!</div>
    );

  }

}

export default Form.create({})(AuthCompletedMention);
